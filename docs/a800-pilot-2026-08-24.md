# A800 node pilot — 2026-08-24

## Verdict

The v0.1 single-node vertical slice passed its infrastructure acceptance on one
broker-managed NVIDIA A800 80GB PCIe card. This is an infrastructure result,
not a custom-kernel speedup result: the fixture compares two equivalent PyTorch
vector expressions only to exercise queueing, gating, timing, and evidence.

## Frozen identity

- Task: `a800-vector-add-smoke`
- Task SHA-256:
  `59453e2cb2b8e23e450fccec44341a19153fff1fcf0699dc99533ac155ffef13`
- Judge source SHA-256:
  `abc84e6abcae270e9f9d735fd85183bbc8abbd528f3700404506c9a517b8adf5`
- Broker-reported version: `0.5.3`
- Managed resource: physical GPU 1 only; GPU 0 remained outside the broker and
  retained its pre-existing processes.

All timestamps below are UTC on 2026-08-24. Run ids resolve under the pilot
state directory's `runs/<run-id>/` tree.

## Shared correctness reuse

Two valid candidates were submitted together and immediately received ids:

- `a800-vector-add-smoke-d419cfb8e10c` started correctness at
  `10:02:30.991342` and finished at `10:02:37.298553`.
- `a800-vector-add-smoke-00fb7e7397fd` started correctness at
  `10:02:30.992200` and finished at `10:02:37.423460`.

The intervals overlap for about 6.3 seconds. Broker status observed physical
GPU 1 as `shared 2/2`, with one job per candidate.

When one slot became available, the intentionally incorrect run
`a800-vector-add-smoke-e968b9079fb6` started its correctness stage at
`10:02:37.321012`. Its judge rejected the candidate, recorded
`validity=invalid`, and no benchmark directory was created.

## Exclusive measurement serialization

- Run `d419cfb8e10c` benchmark: `10:02:39.471145` to `10:02:41.517597`.
- Run `00fb7e7397fd` benchmark: `10:02:41.519381` to `10:02:43.587573`.

The second exclusive benchmark began only after the first finished. Both used
physical GPU 1; no queue or allocation remained after completion.

## Evidence decisions

| Run | Execution | Validity | Frontier decision |
| --- | --- | --- | --- |
| `d419cfb8e10c` | completed | valid | `first-valid`; smoke timing 1.0x |
| `00fb7e7397fd` | completed | valid | `measurement-unstable`; excluded |
| `e968b9079fb6` | rejected after correctness | invalid | `ineligible`; benchmark skipped |

The unstable valid run demonstrates that validity and promotion are separate.
The 1.0x row is a control receipt, not an optimization gain.

## Verification

The same seven dependency-free regression tests passed on the local development
machine and on the A800 node. After the pilot, both tmux-managed services
remained alive, the broker queue was empty, and the managed GPU was idle:

- `kernelinfra-broker-a800`
- `kernelinfra-control-a800`

The run's canonical evidence is its task/candidate snapshots, `events.jsonl`,
per-stage judge results and receipts, aggregate `result.json`, and the derived
task-digest frontier. Console summaries are observations only.

## Remaining gates

- Reconnect to the current KDA Internal owning repository and freeze a wrapper
  against its live judge output; historical output is insufficient.
- Validate the FIBServe service adapter against a broker-held PTXBench deployment
  on supported hardware and a frozen trace set.
- Add cross-host routing only after a second node proves the same receipt and
  cancellation contract. A missing SSH connection must remain `unknown`, never
  a scheduling signal.
