#!/usr/bin/env python3
"""Run the FIBServe adapter from a source checkout."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))

from kernel_infra.adapters.fibserve import main

raise SystemExit(main())
