#!/usr/bin/env python3
"""Run kernelctl from a source checkout without installation."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))

from kernel_infra.cli import main

raise SystemExit(main())
