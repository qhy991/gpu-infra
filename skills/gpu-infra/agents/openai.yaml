interface:
  display_name: "GPU Infra"
  short_description: "Run verified parallel GPU kernel experiments"
  default_prompt: "Use $gpu-infra to submit, monitor, and collect a verified kernel experiment."
